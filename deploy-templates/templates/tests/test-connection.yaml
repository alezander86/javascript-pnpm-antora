apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "javascript-pnpm-antora.fullname" . }}-test-connection"
  labels:
    {{- include "javascript-pnpm-antora.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "javascript-pnpm-antora.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
